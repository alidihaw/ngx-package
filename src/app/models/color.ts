export enum ColorStandard {
    none = "none",
    primary = "primary",
    secondary = "secondary",
    accent = "accent",
    ghost = "ghost",
    link = "link",
    info = "info",
    success = "success",
    warning = "warning",
    error = "error",
}

export enum ColorState {
    primary = "primary",
    info = "info",
    success = "success",
    warn = "warn",
    error = "error",
}